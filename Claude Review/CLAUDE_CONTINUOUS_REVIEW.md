# Claude Continuous Review Log
Last reviewed log entry: IMPLEMENTATION COMPLETE-20260702-2108 (final sweep done; continuous cycle closed)

Reviews of Codex implementation work against Claude Review/CODEX_IMPLEMENTATION_PLAN_REVISED_20260702.md. Entries are appended automatically every 30 minutes when new work appears in Claude Review/CODEX_PROGRESS_LOG.md. Findings are severity-ranked with ACTION FOR CODEX lines; feed them back to Codex in batches.

---

## Review 2026-07-02 19:34 EDT — Entries PHASE-1-20260702-1902, PHASE-2-20260702-1916, PHASE-3-20260702-1925

Files reviewed: scripts/text_safety.py, scripts/requirement_engine.py, scripts/evidence_engine.py, source/Christian_Estrada_Federal_Source.json (full); scripts/build_resume.py, scripts/resume_analysis.py, scripts/build_federal_resume.py (targeted: header dedupe, bullet-ending normalizer, substitution assertions, keyword scoping, audit/injection/gate wiring at lines ~790, ~1488, ~1690, ~2079, ~2609).

Evidence-boundary check: PASS. All four confirmed_evidence entries carry confirmed_on 2026-07-02 and christian-direct-confirmation provenance. The four CONFIRMED_BULLET_TEMPLATES respect ownership ceilings (Aderant "Supported/performed", Aptean "Used", Home Depot bridge-only staffing language, no instance-admin/HA/CI-CD claims). Version-control terminology is gated to Aptean with a negative assertion on the rendered text. No substitution corruption patterns in the new template strings. Grade-equivalence framing present in the most recent role.

Findings:

- S2 (build_federal_resume.py, `_employer_text_block`, ~line 2597): block boundaries are located by the first occurrence of `"{employer},"` in visible text. The East West EFT/ACH bullet contains the literal string "Aptean, and Truist Bank", so when that bullet is selected the East West block is truncated there and the "Aptean block" begins mid-East-West. This can (a) spuriously fail the East West SQL-object injection check if the injected bullet renders after that point, and (b) mask a version-control leak in late East West text from the non-Aptean negative gate, weakening an S1 boundary enforcement. ACTION FOR CODEX: anchor block extraction to the company line (employer + location + "Supervisor:" pattern) or to role-title lines instead of the first `"Employer,"` substring.
- S2 (evidence_engine.py, `load_evidence_catalog`, ~line 139): `CONFIRMED_BULLET_TEMPLATES[record_id]` raises KeyError for any new confirmed_evidence entry, so the phase 3f intake loop cannot land a new confirmation as a data-only source update — it requires a coordinated code edit and crashes if forgotten. ACTION FOR CODEX: read rewrite templates from the source JSON record (e.g. a `rewrite_templates` field) with fallback to the claim text; keep the dict only as an optional override.
- S3 (evidence_engine.py, `never_claim_hits`, ~line 232): record-level `never_claim` lists loaded from the JSON are never checked; only the five global NEVER_CLAIM_PATTERNS run. Also SQL_SERVER_INSTANCE_ADMIN requires "instances|environments", so "administered SQL Server" alone would slip. ACTION FOR CODEX: compile each record's never_claim entries into the rendered-text gate, and drop the instances/environments requirement or add a bare-object variant.
- S3 (build_federal_resume.py, ~line 2079): the grade-equivalence sentence is hard-coded inline rather than "template in config, facts from records" per spec 3c, so it will not generalize past this announcement's scope facts. ACTION FOR CODEX: move the sentence to config with typed slots filled from evidence-record scope facts during phase 4/5 work; acceptable interim.
- S3 (requirement_engine.py, TERM_CANON): bare variants "views" and "functions" canonicalize generic JD language ("essential functions", "views of stakeholders") into SQL-object terms, risking spurious DIRECT matches to the East West record. ACTION FOR CODEX: require SQL context (sql/database/stored/reporting) in the element text before mapping bare "views"/"functions".

Codex's noted deviation (5-page qualifications statement instead of 3) is consistent with the plan's requirement to map all 15 elements plus active supplemental questions; no action.

---

## Review 2026-07-02 ~20:05 EDT — Entry PHASE-4-20260702-1934

Files reviewed: scripts/prose_engine.py (full); scripts/resume_content.py (summary/role-summary repair wiring ~2223, ~2469; role summary templates ~2258); scripts/build_federal_resume.py (build_gs14_summary ~1587); scripts/build_cover_letter.py (repair loop ~5641, DRAFT path ~5653, rule IDs ~5662); scripts/run_resume_workflow.py (~556); scripts/resume_analysis.py (~727, ~1406); scripts/question_prep.py (DRAFT marking); scripts/track_applications.py (tracker lookup ~107); scripts/text_safety.py (collision_safe_substitute, substitution_safety_issues).

Evidence-boundary check: PASS. The new federal summary sentences and role-summary templates contain no version-control, instance-administration, HA, CI/CD, or ownership-ceiling violations; East West "Aptean Intuitive" mentions are product references, not version-control claims. DRAFT-tracker requirement is satisfied indirectly: track_applications discovers resumes only through resume_analysis.matching_output_files, which skips DRAFT stems (~line 727), and run_resume_workflow halts before run_tracker_auto_add when a step reports DRAFT (~556-561).

Findings:

- S2 (prose_engine.py, `_split_long_spoken`, line 123): `" ".join(words[split_at:]).lstrip("and ")` is a character-set strip, not a prefix removal. When the >28-word split falls back to index 22 (no conjunction found), the second sentence's first word loses any leading a/n/d characters: "data analysis..." renders as "Ta analysis...", "analysis" as "Lysis". This produces visibly corrupted words in spoken text across the cheat sheet, detailed guide, and qualifications statement — the same corruption class phase 1 was built to kill. Also, splits landing on "which"/"while" leave the second sentence opening with a bare subordinate conjunction ("Which ..."). ACTION FOR CODEX: replace the lstrip with `re.sub(r"^(?:and|so)\s+", "", second, flags=re.I)` and handle "which/while" splits by dropping or rephrasing the conjunction explicitly; add a regression test with a 30+ word conjunction-free sentence.
- S2 (resume_content.py, `rewrite_professional_summary_for_role` ~2223 and `optimize_role_summaries` ~2469): when repair does not converge, the code silently keeps the ORIGINAL failing text with no DRAFT marking, failure, or feedback. This violates the phase 4 rule "three deterministic repairs then visibly marked DRAFT" on exactly the surface the scope addition named as the S1 evidence (the broken IRS summary). Note the hole is reachable: repair_text has no fixer for SUBSTITUTION_DUPLICATE_GENERIC_TERM (collision_safe_substitute repairs X-to-X and missing articles only), so that finding always ends non-converged and the corrupted summary ships. The federal path handles this correctly (build_gs14_summary fails the build, ~1613). ACTION FOR CODEX: on non-convergence in the commercial summary/role-summary paths, fail the build or route to the DRAFT path with rule IDs; add a mechanical fixer for duplicate generic terms in a sentence.
- S3 (prose_engine.py, `repair_text` ~144): SPOKEN_RESUME_MANDATE is detected case-insensitively but repaired with case-sensitive `str.replace`, so a case-variant mandate sentence detects forever and exits non-converged instead of repaired. ACTION FOR CODEX: use `re.sub(..., flags=re.I)`.
- S3 (prose_engine.py, VALIDATION_RULES): the spec's phase 4 list includes repeated opening verbs, repeated proof clauses, and slot/template overlap; none are in the shared registry. Cover letters retain legacy near-duplicate checks, but summaries now on the assembler have no repeated-opener rule at all. ACTION FOR CODEX: add warn-level registry rules (or record an explicit deferral in the progress log so the gap is tracked).
- S3 (build_cover_letter.py, ~5641): the paragraph repair loop discards RepairOutcome findings/convergence and relies on legacy validators to trigger DRAFT; unresolved prose-engine rule IDs never reach the consolidated `cover_failure_rule_ids` output, which regex-matches legacy failure text only. Consistent with the logged deviation; acceptable until phase 6 cleanup, but rule-ID traceability is lost. ACTION FOR CODEX: when the repair loop ends with fail-severity findings, append their rule IDs to the failure text so the DRAFT feedback names them.

Codex's noted deviation (legacy cover validators retained behind the shared layer until phase 6) is reasonable; see S3 above for the one traceability cost.

---

## Review 2026-07-02 20:36 EDT — Entry PHASE-5-20260702-2013

Files reviewed: prose_engine.py (full); evidence_engine.py (never_claim, catalog load, competency variants, templates); requirement_engine.py (TERM_CANON, canonical_terms); build_federal_resume.py (_employer_text_block ~2602, assert_final_federal_coverage ~2617, grade framing ~2080); question_prep.py (context issues ~606, element probes ~630, DRAFT marking ~657); build_interview_cheat_sheet.py, build_detailed_interview_guide.py, build_standard_qualifications_statement.py (spoken register + DRAFT wiring); resume_content.py (~2223, ~2471); text_safety.py (duplicate-generic repair); tasks.py, build_jd_library.py, job_context_archive.py, run_resume_workflow.py (questions lifecycle); smoke_test.py (split regression ~4557).

Evidence-boundary check: PASS. Confirmed templates unchanged and within ownership ceilings; version-control terms gated to Aptean with a negative rendered-text assertion; SQL_SERVER_INSTANCE_ADMIN now catches bare "administered SQL Server"; record-level never_claim lists now enforced in never_claim_hits. No substitution corruption in new template or probe strings.

Prior findings resolved this cycle: all five from the phase-3 review (block anchoring via company+Supervisor line, rewrite_templates from JSON with dict fallback, record never_claim + bare instance-admin variant, grade sentence moved to config/evidence_rules.py with slot fill from source scope, SQL context required for bare views/functions) and the phase-4 S2/S3 set (regex conjunction strip in _split_long_spoken with a "dollars/Ollars" regression in smoke_test ~4557, case-insensitive mandate repair, warn rules for repeated openers/proof clauses/slot overlap, summary and role-summary non-convergence now fails the build with rule IDs, duplicate-generic-term now mechanically repairable in collision_safe_substitute).

Findings:

- S2 (build_detailed_interview_guide.py `add_answer_box` line 346 and line 441; build_interview_cheat_sheet.py lines 3290, 5058, 5158; build_standard_qualifications_statement.py line 390): every spoken-register call site takes `prose_engine.spoken_register(...).text` and discards findings and convergence. A fail-severity finding the deterministic repairs cannot fix ships silently in a SAY THIS block — reachable because `_repair_and_chain` only fires on 4+ "and" conjunctions while `_nested_list` fires on 4+ commas with 3+ and/or/including, so a comma-heavy nested list with few "and"s loops to non-convergence and is used anyway. This violates the phase-4 rule "three deterministic repairs then visibly marked DRAFT" on spoken surfaces. ACTION FOR CODEX: collect non-converged spoken RepairOutcomes per document and route to the existing DRAFT-marking path with their rule IDs, and/or extend `_repair_and_chain` to also trigger on the nested-list signal.
- S3 (build_federal_resume.py `assert_final_federal_coverage` ~2646): the non-Aptean version-control negative gate matches only slash forms `Azure DevOps/TFS|Git/GitHub` and only runs when "version control" appears in the JD. Injected or edited text saying bare "Git", "GitHub", "TFS", "Azure DevOps", or "version control" under another employer would pass. This is an evidence boundary, not a JD-dependent check. ACTION FOR CODEX: broaden the pattern to `\b(?:azure devops|tfs|git|github|version control)\b` against non-Aptean blocks and run it unconditionally.
- S3 (question_prep.py `application_question_context_issues` ~606): staleness detection is hardcoded to two fixture phrases ("public agencies or cooperatives", "ai product or service"). A leftover prompt naming a different company or domain slips through, and the plan forbids per-JD logic. ACTION FOR CODEX: add a general signal — e.g. flag prompts whose question hash does not match the JD's archived snapshot pairing in job_context_archive, or prompts containing capitalized company/product names absent from the JD — and keep the phrase rules as extras.
- S3 (plan addition 5a, partial): `tasks.py jd-archive` → build_jd_library → `job_context_archive.archive_active_context` does archive application_questions.txt with the snapshot (smoke-tested ~7947), but there is no workflow-runner JD-swap prompt; the stale-question class is caught downstream by DRAFT gates rather than killed at the source. ACTION FOR CODEX: either record this as an accepted deviation in the progress log or add a swap-time check (JD hash changed while questions hash did not → prompt/archive).

Codex's noted deviations (stale active questions intentionally producing DRAFT outputs; 70-page guide length unchanged) are consistent with the plan; the DRAFT quarantine behavior was verified in the workflow, qualifications, interview, and guide paths.

## FINAL SWEEP 2026-07-02 — Entries PHASE-6-20260702-2108 and IMPLEMENTATION COMPLETE-20260702-2108

Files reviewed: commercial_resume_model.py (new, compiles clean, provenance manifest present in scratch/provenance_models/); prose_engine.py, text_safety.py, build_cover_letter.py, resume_content.py, requirement_engine.py, evidence_engine.py, question_prep.py, build_federal_resume.py (all compile clean). Regenerated outputs spot-checked directly.

Acceptance spot checks: PASS. Regenerated IRS federal resume contains "contact center," "stored procedures," and "views" language; Azure DevOps/TFS and Git/GitHub appear only in the Aptean work-experience block (plus the neutral Technical Skills line); no $0 salary; no substitution-corruption fragments. Foundant commercial resume rebuilt on the model at 2 pages with a clean PASS filename; stale-question artifacts correctly quarantined as DRAFT (qualifications, cheat sheet, detailed guide) and excluded from cover/tracker success.

Findings:

- S1 (VERIFY LOCALLY FIRST): in the review sandbox, scripts/build_resume.py reads as truncated mid-file (ends at line 4405 inside `audit_notes=audit_n`) and scripts/smoke_test.py contains a spliced/duplicated registration block (~line 10596), so neither compiles from this view. Evidence strongly suggests OneDrive partial sync rather than real corruption: compiled .pyc caches exist for both, Codex logged 273/273 validate at 21:08, and post-implementation builds succeeded at 20:58-21:08. ACTION FOR CHRISTIAN: run `python tasks.py validate` locally. If it passes, this finding is void (sync artifact). If it fails with a SyntaxError in either file, Codex's final write was interrupted and both files need restoration from git.
- S2 (unresolved from phase-5 review): all six spoken_register call sites (build_detailed_interview_guide.py 346/441, build_interview_cheat_sheet.py 3290/5058/5158, build_standard_qualifications_statement.py 390) still take `.text` and discard findings/convergence; a non-converged fail-severity spoken finding still ships silently. ACTION FOR CODEX: route non-converged spoken RepairOutcomes to the DRAFT path with rule IDs.
- S3 (unresolved): build_federal_resume.py ~2642 version-control negative gate still matches only slash forms and only runs when "version control" appears in the JD. Broaden to bare Git/GitHub/TFS/Azure DevOps tokens against non-Aptean blocks, unconditionally.
- S3 (partially resolved): question_prep.application_question_context_issues now branches by workflow but still detects only fixture phrases; a stale prompt naming a different company/domain passes. Add a general staleness signal via job_context_archive pairing.
- S3 (unresolved, unlogged): no workflow-runner JD-swap check for application_questions.txt and no recorded deviation; the stale-question class is still caught only downstream.
- S3 (new, content register): the model-composed Foundant summary retains the stitched closing "keeping operating continuity as visible as scope, quality, and timeline control" and the "Best in implementations needing..." opener. Grammar rules pass; the sentence bank itself needs a human-register rewrite.

Verdict: five of six phases verified clean against the spec with all evidence boundaries holding across code and regenerated outputs. Phase 6 architecture is sound in every file that syncs consistently. Outstanding work: the S1 local verification plus the five carried findings above, suitable for one small Codex batch when usage resets. Continuous review cycle is CLOSED; the codex-implementation-review scheduled task should be paused.
