# Plan: completing the queued document-quality work

Execution plan for the work still queued after the first pass (F0/F1/F2/F3) shipped. Three items
remain, all documented in existing packets. This plan sequences them and splits the work between
Claude (review and planning) and Codex (implementation and validation) using the repo two-pass
workflow.

## Queued items, in priority order

1. PROSE_NESTED_LIST repair convergence (`Study/Codex_Specs/CODEX_PACKET_prose_nested_list.md`). Highest priority:
   it HARD-FAILS resume builds on the solution-architecture and documentation/training role-summary
   branches. Only a content stopgap (the East West "decision" split) currently protects builds.
2. F4 summaries tightening (`Study/Codex_Specs/CODEX_PACKET_systemwide_docfixes.md`, section F4). Quality.
3. F5 cover letter quality (`Study/Codex_Specs/CODEX_PACKET_systemwide_docfixes.md`, section F5). Quality.

## Roles and a hard constraint on Claude's environment

- Claude (review and plan): design detection rules as code snippets, set thresholds from audit
  data, write golden-test specs, and review Codex diffs. Important: Claude's sandbox cannot run
  full builds reliably (it runs Python 3.10 and sees stale/truncated mounted files), so Claude
  does NOT run the generator. Codex runs every build and audit; Claude consumes the output.
- Codex (implement and validate): implement changes, run the audits across archived JDs, run the
  full smoke suite plus `tasks.py validate` plus a live build, iterate, and commit each item
  separately.

Cross-cutting guardrails (carry forward from the prior packet):
- Word (.docx) output only. Do NOT add any PDF-generation behavior.
- F3 bullet checking stays detect/report-only (already shipped); do not add bullet auto-repair.
- Preserve uncommitted worktree changes; do not revert unrelated edits.
- Every item ends green on `python scripts/smoke_test.py` and `python tasks.py validate`.

## Item 1: PROSE_NESTED_LIST repair convergence

Goal: any sentence the detector flags must be repairable, so decision/solution-architecture/
documentation-training summaries stop hard-failing builds.

- Claude (plan/review, done in packet, refine on request): confirm the fix is a nested-list
  splitter that aligns the repair threshold with the detector (detector fires at 4+ commas and
  3+ of and/or/including; repair currently needs 4+ "and"). Provide the exact 3 regression
  sentences (East West decision, solution-architecture, documentation/training branches). Review
  Codex's diff for idempotence and convergence.
- Codex (implement): add `_repair_nested_list()` and wire it into `repair_text()` for the
  PROSE_NESTED_LIST case; ensure the pass is convergent and idempotent (raise `max_passes` if
  needed). Add the 3 regressions asserting convergence. Run the full smoke suite, including the
  existing assertions near `smoke_test.py:5155` and `:8925`. Once the real fix lands, the East
  West content stopgap in `resume_content.py` can stay (harmless) or be reverted; note the choice.
- Exit criteria: all three known un-repairable sentences converge, no decision/solution-arch/
  doc-training JD hard-fails a build, smoke green.
- Suggested handoff: `python tasks.py claude-packet --mode resume` then
  `python tasks.py claude-prompt review --packet-mode resume` for the review loop.

## Item 2: F4 summaries

Goal: professional and role summaries stay tight (target 45 to 70 words, no sentence over ~30 to
34 words, no double-preposition pileups) while keeping required proof anchors. Reminder: summaries
are generated by `build_problem_first_summary` and `optimized_role_summary`, not read from source.

- Review pass. Claude specifies a `summary` warn rule for `prose_engine` (sentence-length,
  total-length, and 2+ "for"/"to" preposition-pileup checks). Codex implements it at `warn` and
  regenerates summaries across one archived JD per lane (presales_solution, customer_success,
  change_enablement, analytics_operations, corporate_strategy, implementation_delivery), then
  shares the word counts and flags. Claude reads the audit and sets final thresholds and names the
  worst builders.
- Plan pass. Claude specifies the builder edits: dedupe the positioning-plus-context join so
  "for X ... for Y" cannot occur (lines ~2195 to 2203), simplify `woven_context_clause`, and turn
  `ensure_summary_minimum_words` from a hard floor into a target range. Plus golden-test specs.
- Implementation pass. Codex implements the builder edits, reuses the Item 1 nested-list splitter
  to break any over-long first sentence, promotes the rule from warn to fail, adds golden tests
  (per-lane: within word target, no over-cap sentence, no double-for, proof anchors present), and
  runs the full smoke suite.
- Exit criteria: every lane's generated summary is within target with anchors intact; existing
  summary prose-repair assertions stay green.
- Suggested handoff: `claude-packet --mode resume`, `claude-prompt review/plan --packet-mode resume`.

## Item 3: F5 cover letter quality

Goal: coherent opener (F1 already fixed), 2 to 3 substantive body paragraphs each with a distinct
proof, no internal lane tokens in prose, a company-specific hook, and an honest bridge sentence
for BRIDGE/FAIL fits.

- Review pass. Claude specifies two lints for `build_cover_letter.py`: an internal-lane-token
  detector (rejects prose containing tokens like `presales_solution`, `change_enablement`,
  "role-based enablement and adoption work") and a body-substance check (2 to 3 paragraphs, each
  with a distinct proof). Codex adds them at warn and runs across archived JDs including the
  Randstad snapshot, then shares leaks and thin-body cases. Claude reviews.
- Plan pass. Claude specifies: guarantee 2 to 3 body paragraphs with a canonical-proof fallback
  when selection under-fills; replace the lane-label sentence template with JD-noun phrasing; add
  a company hook from `jobs/company_notes` or `jobs/company_research.txt` with a safe fallback;
  and generate an honest bridge sentence when the resume notes flag bridge gaps. Plus golden-test
  specs.
- Implementation pass. Codex implements, runs every cover paragraph through `prose_engine` "cover"
  repair plus the F4 length/preposition rule, adds golden tests (coherent single-sentence opener,
  2 to 3 distinct-proof paragraphs, zero internal tokens, company reference present), and runs the
  full smoke suite.
- Exit criteria: generated letters for 3 to 4 archived JDs pass all golden checks; existing cover
  assertions stay green.
- Suggested handoff: `claude-packet --mode cover`, `claude-prompt review/plan --packet-mode cover`.

## Milestones

- M1: Item 1 merged. Builds can no longer hard-fail on nested-list summaries.
- M2: Item 2 merged. Summaries tightened system-wide, rule enforced.
- M3: Item 3 merged. Cover letters substantive and lane-token-free, rule enforced.

Do the items strictly in order: Item 1 removes a build-breaker and produces the nested-list
splitter that Items 2 and 3 reuse.

## Optional, small, separate

"core training" truthful weave for the Randstad resume: only if the owner confirms it is
supportable by real training experience. One source edit plus a rebuild; not part of the generator
work above. Do not force "policy training", "strategic delivery", or "delivery-based", which are
correctly reported as unsupported.
