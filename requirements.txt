# Christian Resume System Dependencies
# Pinned versions to ensure API stability and compatibility
# Runtime: Python 3.11 or newer is required.

# Core document processing
python-docx==1.2.0

# Note: The system uses primarily Python standard library modules
# (re, json, pathlib, xml.etree, subprocess, zipfile, etc.)
# No other external dependencies are required for the core functionality.
